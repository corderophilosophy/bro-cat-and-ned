import React, { Component, PropTypes } from 'react';

export default class About extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (<h1 className="mt0 pt6">About Page</h1>);
  }
}

About.propTypes = {
};
